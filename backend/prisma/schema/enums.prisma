// -----------------------------------------------------------------------------
// Shared enums
// -----------------------------------------------------------------------------

enum InventorySyncStatus {
  running
  success
  partial_success
  failed
}

enum InventorySourceSyncStatus {
  success
  failed
}

enum PermissionEffect {
  ALLOW
  DENY
}
